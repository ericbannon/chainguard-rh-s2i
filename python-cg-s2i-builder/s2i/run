#!/bin/sh
set -euo pipefail
cd /opt/app

PORT="${PORT:-8080}"
VENV_DIR="/opt/app/venv"

# run gunicorn from the venv (no reliance on PATH)
exec "$VENV_DIR/bin/python" -m gunicorn -b "0.0.0.0:${PORT}" app:app